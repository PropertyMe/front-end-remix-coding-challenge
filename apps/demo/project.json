{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "demo",
  "sourceRoot": "apps/demo",
  "projectType": "application",
  "tags": [],
  "targets": {
    "tsc": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "npx tsc --noEmit -p apps/demo/tsconfig.json"
          }
        ]
      }
    }
  }
}
